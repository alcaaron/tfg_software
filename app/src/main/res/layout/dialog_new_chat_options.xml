<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center"
    android:paddingHorizontal="24dp"
    android:paddingTop="28dp"
    android:paddingBottom="20dp">

    <ImageButton
        android:id="@+id/optionSendNode"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_marginHorizontal="16dp"
        android:background="@drawable/circle_primary"
        android:src="@drawable/ic_paper_plane"
        android:scaleType="fitCenter"
        android:padding="14dp"
        android:contentDescription="Mensaje a un nodo"/>

    <ImageButton
        android:id="@+id/optionJoinGroup"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_marginHorizontal="16dp"
        android:background="@drawable/circle_primary"
        android:src="@drawable/ic_group_join"
        android:scaleType="fitCenter"
        android:padding="14dp"
        android:contentDescription="Unirse a grupo"/>

    <ImageButton
        android:id="@+id/optionCreateGroup"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_marginHorizontal="16dp"
        android:background="@drawable/circle_primary"
        android:src="@drawable/ic_group_create"
        android:scaleType="fitCenter"
        android:padding="14dp"
        android:contentDescription="Crear grupo"/>

</LinearLayout>
